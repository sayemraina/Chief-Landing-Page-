<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Only read the relevant guide in `node_modules/next/dist/docs/` when working with Next.js internals: routing, middleware, config, or unfamiliar Next.js APIs. Do NOT read it for component edits, styling changes, or copy updates.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->
